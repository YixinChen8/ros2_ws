<?xml version="1.0" encoding="UTF-8"?>
<testsuite
  name="polygon_base.uncrustify"
  tests="2"
  failures="2"
  errors="0"
  time="0.044"
>
  <testcase
    name="include/polygon_base/regular_polygon.hpp"
    classname="polygon_base.uncrustify"
  >
      <failure message="Diff with 30 lines"><![CDATA[--- include/polygon_base/regular_polygon.hpp
+++ include/polygon_base/regular_polygon.hpp.uncrustify
@@ -7,5 +7,5 @@
-  class RegularPolygon
-  {
-    public:
-    // One thing to notice is the presence of the initialize method. 
-    // With pluginlib, a constructor without parameters is required, 
+class RegularPolygon
+{
+public:
+    // One thing to notice is the presence of the initialize method.
+    // With pluginlib, a constructor without parameters is required,
@@ -13,3 +13,3 @@
-      virtual void initialize(double side_length) = 0;
-      virtual double area() = 0;
-      virtual ~RegularPolygon(){}
+  virtual void initialize(double side_length) = 0;
+  virtual double area() = 0;
+  virtual ~RegularPolygon() {}
@@ -17,3 +17,3 @@
-    protected:
-      RegularPolygon(){}
-  };
+protected:
+  RegularPolygon() {}
+};
@@ -22 +22 @@
-#endif  // POLYGON_BASE_REGULAR_POLYGON_HPP+#endif  // POLYGON_BASE_REGULAR_POLYGON_HPP
]]></failure>
  </testcase>
  <testcase
    name="src/area_node.cpp"
    classname="polygon_base.uncrustify"
  >
      <failure message="Diff with 38 lines"><![CDATA[--- src/area_node.cpp
+++ src/area_node.cpp.uncrustify
@@ -16 +16 @@
-int main(int argc, char** argv)
+int main(int argc, char ** argv)
@@ -28 +28,2 @@
-  pluginlib::ClassLoader<polygon_base::RegularPolygon> poly_loader("polygon_base", "polygon_base::RegularPolygon");
+  pluginlib::ClassLoader<polygon_base::RegularPolygon> poly_loader("polygon_base",
+    "polygon_base::RegularPolygon");
@@ -30,2 +31 @@
-  try
-  {
+  try {
@@ -33,4 +33,4 @@
-    There are a number of ways to instantiate an instance of the class. In this example, we’re using shared pointers. 
-    We just need to call createSharedInstance with a reference to the plugin: 
-    This can be either the fully-qualified type of the plugin class (the type attribute of the declaration XML file, e.g. polygon_plugins::Square), 
-    or the optional magic name (the name attribute of the declaration XML file, e.g., awesome_triangle).    
+    There are a number of ways to instantiate an instance of the class. In this example, we’re using shared pointers.
+    We just need to call createSharedInstance with a reference to the plugin:
+    This can be either the fully-qualified type of the plugin class (the type attribute of the declaration XML file, e.g. polygon_plugins::Square),
+    or the optional magic name (the name attribute of the declaration XML file, e.g., awesome_triangle).
@@ -38 +38,2 @@
-    std::shared_ptr<polygon_base::RegularPolygon> triangle = poly_loader.createSharedInstance("awesome_triangle");
+    std::shared_ptr<polygon_base::RegularPolygon> triangle =
+      poly_loader.createSharedInstance("awesome_triangle");
@@ -41 +42,2 @@
-    std::shared_ptr<polygon_base::RegularPolygon> square = poly_loader.createSharedInstance("polygon_plugins::Square");
+    std::shared_ptr<polygon_base::RegularPolygon> square =
+      poly_loader.createSharedInstance("polygon_plugins::Square");
@@ -46,3 +48 @@
-  }
-  catch(pluginlib::PluginlibException& ex)
-  {
+  } catch(pluginlib::PluginlibException & ex) {
@@ -53 +53 @@
-}+}
]]></failure>
  </testcase>
  <system-out>Checked files:
* include/polygon_base/regular_polygon.hpp
* src/area_node.cpp</system-out>
</testsuite>
